services:
  web:
    build: .
    ports:
      - "3300:3000"
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
    environment:
      RAILS_ENV: development
      SECRET_KEY_BASE: development_secret_key_base_please_change_1234567890

volumes:
  bundle_cache:
